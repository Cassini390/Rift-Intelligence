# LoL-Tracker
Personal stat tracker for League of Legends

Place the index.html, package.json and the server.js into a folder, 
then run a console within the folder and run:

    npm install

Then run a console from within the folder and run

    node server.js
